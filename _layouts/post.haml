---
layout: default
---
#post
  =header page.title
  %p.subtitle
    Written
    = page.date.strftime("%B %d, %Y at %H:%M %Z"), '.'
    Tagged
    = tag_links(page.tags), "."

    -# TextMate edit link. JS sets the link text if a cookie is present: see js/app.js.
    %span#edit= link_to('', "txmt://open?url=file://#{page.path}")
   
  -# Without the squiggly, Haml will indent highlighted code. 
  ~ content
  
  #older_and_newer
    - if page.next
      %p
        %strong Newer:
        =post_link(page.next)
    - if page.previous
      %p
        %strong Older:
        =post_link(page.previous)
  
  #disqus_thread
  %script{ :type => "text/javascript", :src => "http://disqus.com/forums/pug/embed.js" }
  %noscript <a href="http://pug.disqus.com/?url=ref">View the discussion thread.</a>
